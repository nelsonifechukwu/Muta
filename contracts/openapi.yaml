openapi: 3.1.0
info:
  title: Muta — Offline Adaptive Tutor
  description: ADTC 2026. The only public surface is the versioned /v1 contract; every client — browser,
    phones, eval.py, CLI — speaks only /v1.
  version: 0.1.0
paths:
  /v1/health:
    get:
      tags:
      - ops
      summary: Health
      operationId: health_v1_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /v1/ready:
    get:
      tags:
      - ops
      summary: Ready
      description: 'Probes engine + db directly from config, never through `get_engine()` — readiness

        must report a down dependency, not hang constructing a store against it. Always HTTP

        200; the compose healthcheck greps the body for `"ready":true`.'
      operationId: ready_v1_ready_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadyResponse'
  /v1/chat:
    post:
      tags:
      - tutor
      summary: Chat
      description: 'Multi-turn tutoring turn. Memory is keyed by `conversation_id`; omit it to start a

        new thread. The mode selects the system prompt (ROADMAP 18 Jul, stable-prefix design).'
      operationId: chat_v1_chat_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/chat/stream:
    post:
      tags:
      - tutor
      summary: Chat Stream
      description: 'Token-streaming twin of `/chat` — the browser UI''s primary path.


        Emits Server-Sent Events: `{"reasoning": "..."}` for Qwen3 thinking tokens and

        `{"delta": "..."}` for answer tokens, then a final

        `{"done": true, "conversation_id", "completion_tokens", "elapsed_s", "tokens_per_second"}`.'
      operationId: chat_stream_v1_chat_stream_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
        required: true
      responses:
        '200':
          description: SSE token stream
          content:
            application/json:
              schema: {}
            text/event-stream: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/conversations:
    get:
      tags:
      - conversations
      summary: Conversations
      description: A student's threads, most recently active first — the UI sidebar.
      operationId: conversations_v1_conversations_get
      parameters:
      - name: student_id
        in: query
        required: true
        schema:
          type: string
          title: Student Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationList'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/conversations/{conversation_id}/messages:
    get:
      tags:
      - conversations
      summary: Conversation Messages
      description: Full history with attachment refs — how the UI reloads a thread after a restart.
      operationId: conversation_messages_v1_conversations__conversation_id__messages_get
      parameters:
      - name: conversation_id
        in: path
        required: true
        schema:
          type: string
          title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageList'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/conversations/{conversation_id}/telemetry:
    get:
      tags:
      - ops
      summary: Conversation Telemetry
      description: One-shot telemetry snapshot (curl-able twin of the SSE stream below).
      operationId: conversation_telemetry_v1_conversations__conversation_id__telemetry_get
      parameters:
      - name: conversation_id
        in: path
        required: true
        schema:
          type: string
          title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TelemetrySnapshot'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/conversations/{conversation_id}/telemetry/stream:
    get:
      tags:
      - ops
      summary: Conversation Telemetry Stream
      description: '1 Hz telemetry SSE. Async generator on purpose: a sync generator would pin a

        threadpool thread per open strip. GET, so the browser''s native EventSource works.'
      operationId: conversation_telemetry_stream_v1_conversations__conversation_id__telemetry_stream_get
      parameters:
      - name: conversation_id
        in: path
        required: true
        schema:
          type: string
          title: Conversation Id
      responses:
        '200':
          description: 1 Hz SSE telemetry
          content:
            application/json:
              schema: {}
            text/event-stream: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/conversations/{conversation_id}:
    delete:
      tags:
      - conversations
      summary: Conversation Delete
      operationId: conversation_delete_v1_conversations__conversation_id__delete
      parameters:
      - name: conversation_id
        in: path
        required: true
        schema:
          type: string
          title: Conversation Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationDeleted'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/attachments/{attachment_id}:
    get:
      tags:
      - conversations
      summary: Attachment
      operationId: attachment_v1_attachments__attachment_id__get
      parameters:
      - name: attachment_id
        in: path
        required: true
        schema:
          type: integer
          title: Attachment Id
      responses:
        '200':
          description: Raw bytes
          content:
            application/octet-stream: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/diagnose:
    post:
      tags:
      - tutor
      summary: Diagnose
      operationId: diagnose_v1_diagnose_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiagnoseRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiagnoseResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/generate_question:
    post:
      tags:
      - exam
      summary: Generate Question
      operationId: generate_question_v1_generate_question_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateQuestionRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerateQuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/mastery/{student_id}:
    get:
      tags:
      - tutor
      summary: Mastery
      operationId: mastery_v1_mastery__student_id__get
      parameters:
      - name: student_id
        in: path
        required: true
        schema:
          type: string
          title: Student Id
      - name: subject
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Subject'
          default: math
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MasteryResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/verify:
    post:
      tags:
      - math
      summary: Verify
      description: 'Check a claim of the form `lhs == rhs` (or `lhs = rhs`) in the SymPy sandbox.


        The single-expression shape this endpoint has always had; `/tutor/verify` is the

        two-sided form the tool loop uses.'
      operationId: verify_v1_verify_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifyResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/tutor/chat:
    post:
      tags:
      - tutor
      summary: Tutor Chat
      description: 'One tutoring turn, admission-controlled (§8.2) and ladder-aware (§5.3).


        Set `stream: false` for this JSON shape; `stream: true` (the default) is served by

        `/tutor/chat/stream`, which speaks SSE. Both take the same body.'
      operationId: tutor_chat_v1_tutor_chat_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatTurn'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TutorReply'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/tutor/chat/stream:
    post:
      tags:
      - tutor
      summary: Tutor Chat Stream
      description: 'Token-streaming twin of `/tutor/chat` (§7.2: the tutoring turn is SSE).


        Events: `{"reasoning": …}` while the model thinks, `{"delta": …}` per answer token, then

        a final `{"done": true, …}`. Time-to-first-token is the number a student feels (SC-3:

        < 2.5 s), so the stream starts before the answer is finished, not after.'
      operationId: tutor_chat_stream_v1_tutor_chat_stream_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatTurn'
        required: true
      responses:
        '200':
          description: SSE token stream
          content:
            application/json:
              schema: {}
            text/event-stream: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/tutor/vision:
    post:
      tags:
      - tutor
      summary: Tutor Vision
      description: 'Photo of handwritten work → transcription (S2).


        Two refusals, both friendly: the image guard (too big, wrong format) and the ladder

        (no memory for a vision instance). Neither is an error — the student is told to type the

        problem instead, and the tutor keeps working.'
      operationId: tutor_vision_v1_tutor_vision_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_tutor_vision_v1_tutor_vision_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VisionReply'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/tutor/verify:
    post:
      tags:
      - math
      summary: Tutor Verify
      description: 'Candidate vs expected, via SymPy in the sandbox (§7.5).


        `checked=False` means no verdict was possible (no answer found, sandbox down) — the

        caller must not report that as "wrong".'
      operationId: tutor_verify_v1_tutor_verify_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnswerCheckRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnswerCheckResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/tutor/render:
    post:
      tags:
      - tutor
      summary: Tutor Render
      description: 'Model-emitted plotting code → SVG, sandboxed (§7.5, S5). A failure returns `ok=False`

        with the error, never a broken image.'
      operationId: tutor_render_v1_tutor_render_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RenderRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RenderResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/session/{session_id}/suspend:
    post:
      tags:
      - ops
      summary: Session Suspend
      description: Persist a session's KV and free its slot (§8.3).
      operationId: session_suspend_v1_session__session_id__suspend_post
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionActionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/session/{session_id}/resume:
    post:
      tags:
      - ops
      summary: Session Resume
      description: Bind a slot for a session, restoring its snapshot when one survives.
      operationId: session_resume_v1_session__session_id__resume_post
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionActionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /v1/metrics:
    get:
      tags:
      - ops
      summary: Metrics
      description: 'Local health panel data (§12). All local: no exporter, no network, no dashboards.'
      operationId: metrics_v1_metrics_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStatus'
components:
  schemas:
    AnswerCheckRequest:
      properties:
        candidate:
          type: string
          title: Candidate
          description: The answer to check — bare, boxed, or inside prose.
        expected:
          type: string
          title: Expected
        tolerance:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Tolerance
          description: Relative tolerance; 0 = exact.
          default: 0.0
      type: object
      required:
      - candidate
      - expected
      title: AnswerCheckRequest
    AnswerCheckResponse:
      properties:
        verified:
          type: boolean
          title: Verified
        checked:
          type: boolean
          title: Checked
          description: False when no verdict was possible — not the same as wrong.
        normalized_candidate:
          type: string
          title: Normalized Candidate
          default: ''
        normalized_expected:
          type: string
          title: Normalized Expected
          default: ''
        detail:
          type: string
          title: Detail
          default: ''
      type: object
      required:
      - verified
      - checked
      title: AnswerCheckResponse
    AttachmentRef:
      properties:
        id:
          type: integer
          title: Id
        kind:
          type: string
          enum:
          - image
          - audio
          title: Kind
        mime:
          type: string
          title: Mime
      type: object
      required:
      - id
      - kind
      - mime
      title: AttachmentRef
    Body_tutor_vision_v1_tutor_vision_post:
      properties:
        session_id:
          type: string
          title: Session Id
        image:
          type: string
          contentMediaType: application/octet-stream
          title: Image
        conversation_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Conversation Id
      type: object
      required:
      - session_id
      - image
      title: Body_tutor_vision_v1_tutor_vision_post
    ChatRequest:
      properties:
        student_id:
          type: string
          title: Student Id
          description: Stable per-learner id; keys the learning twin.
        message:
          type: string
          title: Message
        conversation_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Conversation Id
          description: Omit to start a new thread; pass to continue one (multi-turn memory).
        mode:
          $ref: '#/components/schemas/TutoringMode'
          default: socratic
        persona:
          $ref: '#/components/schemas/Persona'
          default: teacher
        subject:
          $ref: '#/components/schemas/Subject'
          default: math
        language:
          type: string
          title: Language
          description: 'BCP-47-ish tag: en, fr, ha, yo, ig, sw, ar, am, zu.'
          default: en
        stream:
          type: boolean
          title: Stream
          description: When true the server replies with an SSE token stream instead.
          default: false
        attachment_ids:
          items:
            type: integer
          type: array
          title: Attachment Ids
          description: Previously-uploaded attachments to link to this message.
      type: object
      required:
      - student_id
      - message
      title: ChatRequest
    ChatResponse:
      properties:
        student_id:
          type: string
          title: Student Id
        conversation_id:
          type: string
          title: Conversation Id
          description: Pass back in the next request to continue the thread.
        mode:
          $ref: '#/components/schemas/TutoringMode'
        reply:
          type: string
          title: Reply
        verified:
          type: boolean
          title: Verified
          description: Whether math in the reply was checked by the `math` service.
          default: false
        citations:
          items:
            type: string
          type: array
          title: Citations
          description: RAG source references.
      type: object
      required:
      - student_id
      - conversation_id
      - mode
      - reply
      title: ChatResponse
    ChatTurn:
      properties:
        session_id:
          type: string
          title: Session Id
          description: One student's tutoring session; keys slot + twin.
        text:
          type: string
          maxLength: 4096
          title: Text
          description: 'Input cap per turn (S12: prompt-bomb guard).'
        mode:
          $ref: '#/components/schemas/TutorMode'
          default: dialogue
        lang:
          type: string
          title: Lang
          default: en
        student_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Student Id
          description: Defaults to session_id when omitted.
        stream:
          type: boolean
          title: Stream
          description: Kept for client symmetry. Token streaming has its own path — POST /v1/tutor/chat/stream
            (text/event-stream) — because one operation cannot honestly declare two response media types
            in the schema.
          default: false
      type: object
      required:
      - session_id
      - text
      title: ChatTurn
    ConversationDeleted:
      properties:
        id:
          type: string
          title: Id
        deleted:
          type: boolean
          title: Deleted
          default: true
      type: object
      required:
      - id
      title: ConversationDeleted
    ConversationList:
      properties:
        conversations:
          items:
            $ref: '#/components/schemas/ConversationOut'
          type: array
          title: Conversations
      type: object
      title: ConversationList
    ConversationOut:
      properties:
        id:
          type: string
          title: Id
        student_id:
          type: string
          title: Student Id
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
        mode:
          anyOf:
          - type: string
          - type: 'null'
          title: Mode
        created_at:
          type: string
          title: Created At
        updated_at:
          type: string
          title: Updated At
      type: object
      required:
      - id
      - student_id
      - created_at
      - updated_at
      title: ConversationOut
    DiagnoseRequest:
      properties:
        student_id:
          type: string
          title: Student Id
        subject:
          $ref: '#/components/schemas/Subject'
          default: math
        topic:
          anyOf:
          - type: string
          - type: 'null'
          title: Topic
          description: Optional focus; otherwise diagnose broadly.
      type: object
      required:
      - student_id
      title: DiagnoseRequest
    DiagnoseResponse:
      properties:
        student_id:
          type: string
          title: Student Id
        weak_topics:
          items:
            type: string
          type: array
          title: Weak Topics
        plan:
          items:
            type: string
          type: array
          title: Plan
          description: Concrete next steps (e.g. 7-day).
      type: object
      required:
      - student_id
      title: DiagnoseResponse
    GenerateQuestionRequest:
      properties:
        subject:
          $ref: '#/components/schemas/Subject'
          default: math
        topic:
          type: string
          title: Topic
        difficulty:
          type: integer
          maximum: 5.0
          minimum: 1.0
          title: Difficulty
          default: 3
        exam_board:
          type: string
          title: Exam Board
          default: WAEC
        count:
          type: integer
          maximum: 20.0
          minimum: 1.0
          title: Count
          default: 1
      type: object
      required:
      - topic
      title: GenerateQuestionRequest
    GenerateQuestionResponse:
      properties:
        questions:
          items:
            $ref: '#/components/schemas/GeneratedQuestion'
          type: array
          title: Questions
      type: object
      title: GenerateQuestionResponse
    GeneratedQuestion:
      properties:
        question_text:
          type: string
          title: Question Text
        options:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Options
          description: Present for objective (Paper 1) items.
        correct_answer:
          type: string
          title: Correct Answer
        worked_solution:
          type: string
          title: Worked Solution
        marking_scheme:
          anyOf:
          - type: string
          - type: 'null'
          title: Marking Scheme
          description: Method + answer marks (Paper 2).
        topic_tags:
          items:
            type: string
          type: array
          title: Topic Tags
        difficulty:
          type: integer
          maximum: 5.0
          minimum: 1.0
          title: Difficulty
          default: 3
      type: object
      required:
      - question_text
      - correct_answer
      - worked_solution
      title: GeneratedQuestion
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    HealthResponse:
      properties:
        status:
          type: string
          const: ok
          title: Status
          default: ok
        service:
          type: string
          title: Service
      type: object
      required:
      - service
      title: HealthResponse
    MasteryResponse:
      properties:
        student_id:
          type: string
          title: Student Id
        subject:
          $ref: '#/components/schemas/Subject'
        mastery:
          additionalProperties:
            type: number
          type: object
          title: Mastery
          description: topic_id -> mastery in [0, 1] on the curriculum DAG.
        next_topic:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Topic
          description: Next-best lesson given prerequisites.
      type: object
      required:
      - student_id
      - subject
      title: MasteryResponse
    MessageList:
      properties:
        conversation_id:
          type: string
          title: Conversation Id
        messages:
          items:
            $ref: '#/components/schemas/MessageOut'
          type: array
          title: Messages
      type: object
      required:
      - conversation_id
      title: MessageList
    MessageOut:
      properties:
        id:
          type: integer
          title: Id
        role:
          type: string
          title: Role
        content:
          type: string
          title: Content
        created_at:
          type: string
          title: Created At
        attachments:
          items:
            $ref: '#/components/schemas/AttachmentRef'
          type: array
          title: Attachments
      type: object
      required:
      - id
      - role
      - content
      - created_at
      title: MessageOut
    Persona:
      type: string
      enum:
      - teacher
      - friend
      - professor
      - exam
      title: Persona
    ReadyResponse:
      properties:
        ready:
          type: boolean
          title: Ready
        checks:
          additionalProperties:
            type: boolean
          type: object
          title: Checks
          description: Per-dependency readiness (llama-server, sub-apps).
      type: object
      required:
      - ready
      title: ReadyResponse
    RenderRequest:
      properties:
        kind:
          type: string
          enum:
          - matplotlib
          - svg
          title: Kind
          default: matplotlib
        code:
          type: string
          maxLength: 8192
          title: Code
      type: object
      required:
      - code
      title: RenderRequest
    RenderResponse:
      properties:
        svg:
          type: string
          title: Svg
          default: ''
        ok:
          type: boolean
          title: Ok
          default: true
        error:
          type: string
          title: Error
          default: ''
        fallback_text:
          type: string
          title: Fallback Text
          description: Description to show when rendering failed (S5).
          default: ''
      type: object
      title: RenderResponse
    SessionActionResponse:
      properties:
        session_id:
          type: string
          title: Session Id
        action:
          type: string
          enum:
          - suspend
          - resume
          title: Action
        ok:
          type: boolean
          title: Ok
        detail:
          type: string
          title: Detail
          default: ''
      type: object
      required:
      - session_id
      - action
      - ok
      title: SessionActionResponse
    Subject:
      type: string
      enum:
      - math
      - physics
      - chemistry
      - biology
      title: Subject
    SystemStatus:
      properties:
        degradation:
          additionalProperties: true
          type: object
          title: Degradation
        sessions:
          additionalProperties: true
          type: object
          title: Sessions
        vision:
          additionalProperties: true
          type: object
          title: Vision
        engine:
          additionalProperties: true
          type: object
          title: Engine
      type: object
      title: SystemStatus
      description: '`/v1/metrics` — the local health panel''s data (TDD §12). All local, no exporter.'
    TelemetrySnapshot:
      properties:
        rss_gb:
          type: number
          title: Rss Gb
          description: Current RSS of the whole backend process tree.
        peak_rss_gb:
          type: number
          title: Peak Rss Gb
          description: Peak tree RSS since the backend started.
        cpu_temp_c:
          anyOf:
          - type: number
          - type: 'null'
          title: Cpu Temp C
          description: CPU package temp; null if unreadable.
        throttled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Throttled
          description: temp > 85 °C; null when the temperature is unreadable.
        tokens_per_second:
          anyOf:
          - type: number
          - type: 'null'
          title: Tokens Per Second
          description: Rolling decode rate of this conversation's active generation.
        generating:
          type: boolean
          title: Generating
          default: false
      type: object
      required:
      - rss_gb
      - peak_rss_gb
      title: TelemetrySnapshot
      description: 'Per-conversation live telemetry. Unmeasurable metrics are null (the UI shows —),

        never an error: Docker-on-macOS has no CPU temperature, for example.'
    TutorMode:
      type: string
      enum:
      - dialogue
      - solution
      - marking
      - hint
      title: TutorMode
      description: 'TDD Appendix D. `dialogue` and `solution` differ in sampling (§6.5), `marking` also
        in

        grammar, `hint` also in which model answers (§7.6).'
    TutorReply:
      properties:
        session_id:
          type: string
          title: Session Id
        reply:
          type: string
          title: Reply
        mode:
          $ref: '#/components/schemas/TutorMode'
        verified:
          type: boolean
          title: Verified
          default: false
        citations:
          items:
            type: string
          type: array
          title: Citations
        queued:
          type: boolean
          title: Queued
          description: True when the turn waited for a slot (§8.2).
          default: false
        queue_position:
          type: integer
          title: Queue Position
          default: 0
        degradation_level:
          type: string
          title: Degradation Level
          description: Ladder level at the time of the turn (§5.3).
          default: L0
      type: object
      required:
      - session_id
      - reply
      - mode
      title: TutorReply
    TutoringMode:
      type: string
      enum:
      - socratic
      - subgoal
      title: TutoringMode
      description: The two MVP tutoring modes (ROADMAP 14 Jul scope).
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    VerifyRequest:
      properties:
        expression:
          type: string
          title: Expression
          description: A claim/step to check, e.g. 'd/dx(x^2) == 2*x'.
        context:
          anyOf:
          - type: string
          - type: 'null'
          title: Context
          description: Optional assumptions/variable domains.
      type: object
      required:
      - expression
      title: VerifyRequest
    VerifyResponse:
      properties:
        verified:
          type: boolean
          title: Verified
        normalized:
          anyOf:
          - type: string
          - type: 'null'
          title: Normalized
          description: Canonical form the checker computed.
        detail:
          anyOf:
          - type: string
          - type: 'null'
          title: Detail
          description: Why it failed, when it did.
      type: object
      required:
      - verified
      title: VerifyResponse
    VisionReply:
      properties:
        session_id:
          type: string
          title: Session Id
        transcription:
          type: string
          title: Transcription
          description: What the model read from the image.
          default: ''
        analysis:
          type: string
          title: Analysis
          default: ''
        accepted:
          type: boolean
          title: Accepted
          description: False when the image guard or the ladder refused.
          default: true
        detail:
          type: string
          title: Detail
          description: Why it was refused, in words a student can act on.
          default: ''
        attachment_id:
          anyOf:
          - type: integer
          - type: 'null'
          title: Attachment Id
          description: Stored image attachment; pass in ChatRequest.attachment_ids.
      type: object
      required:
      - session_id
      title: VisionReply
