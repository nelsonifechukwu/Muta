# KV budget — Qwen3-0.6B-Q4_K_M.gguf

Generated by `python -m runtime.kvmath models/Qwen3-0.6B/Qwen3-0.6B-Q4_K_M.gguf` (TDD T5, §5.2). Do not hand-edit: this
table is derived from the shipped file's own metadata, which is the entire point — the TDD's
version used representative Qwen-family values, and representative is not measured.

Model: arch `qwen3`, 28 layers, 8 KV heads,
head_dim 128,
trained context 40960, file 0.37 GiB.

Per-token KV = n_layer × n_kv_head × (head_dim_k × B_k + head_dim_v × B_v)
= 28 × 8 × (128 + 128) elements
= **57,344 elements/token**.

## KV quantization ladder (D6)

| cache type | per token | 4096-token slot |
|---|---|---|
| f16 | 112.0 KiB | 448 MiB |
| q8_0 | 59.5 KiB | 238 MiB |
| q5_1 | 42.0 KiB | 168 MiB |

## Slot budget vs the CORE-TEXT cap (4300 MiB)

| profile | `-c` | `-np` | ctx/slot | KV | weights | buffers | total | fits? |
|---|---|---|---|---|---|---|---|---|
| classroom | 12288 | 6 | 2048 | 714 MiB | 378 MiB | 600 MiB | 1692 MiB | yes |
| classroom-6 | 24576 | 6 | 4096 | 1428 MiB | 378 MiB | 600 MiB | 2406 MiB | yes |
| classroom-8 | 32768 | 8 | 4096 | 1904 MiB | 378 MiB | 600 MiB | 2882 MiB | yes |
| solo-demo | 8192 | 2 | 4096 | 476 MiB | 378 MiB | 600 MiB | 1454 MiB | yes |

> Compute-buffer column is PROVISIONAL (600 MiB at `-ub 512`, scaled linearly). [MEASURE: RSS delta across the `-ub` sweep on the target box, T13] — re-run with `--buffers-mib` once measured.

