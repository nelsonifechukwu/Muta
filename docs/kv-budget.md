# KV budget — Qwen3.5-4B-Q4_K_M.gguf

Generated by `python -m runtime.kvmath models/core/Qwen3.5-4B-Q4_K_M.gguf` (TDD T5, §5.2). Do not hand-edit: this
table is derived from the shipped file's own metadata, which is the entire point — the TDD's
version used representative Qwen-family values, and representative is not measured.

Model: arch `qwen35`, 32 layers, 4 KV heads,
head_dim 256,
trained context 262144, file 2.55 GiB.

Per-token KV = n_layer × n_kv_head × (head_dim_k × B_k + head_dim_v × B_v)
= 8 × 4 × (256 + 256) elements
= **16,384 elements/token**.

Hybrid layout: **8 attention** layers (token-growing KV) + **24 recurrent** layers at a constant **50.3 MiB f32 per slot** (and per context checkpoint). Slot totals below charge the state ONCE per slot; each context checkpoint copies it again — worst case per slot = state × (1 + ctx_checkpoints), engine default 32, capped to 2 via `MUTA_RT_CTX_CHECKPOINTS` on the gateway path (`runtime/profiles.py` launch paths pass no cap).

## KV quantization ladder (D6)

| cache type | per token | 4096-token slot |
|---|---|---|
| f16 | 32.0 KiB | 128 MiB |
| q8_0 | 17.0 KiB | 68 MiB |
| q5_1 | 12.0 KiB | 48 MiB |

## Slot budget vs the CORE-TEXT cap (4300 MiB)

| profile | `-c` | `-np` | ctx/slot | KV | state | weights | buffers | total | fits? |
|---|---|---|---|---|---|---|---|---|---|
| classroom | 12288 | 6 | 2048 | 204 MiB | 302 MiB | 2614 MiB | 600 MiB | 3719 MiB | yes |
| classroom-6 | 24576 | 6 | 4096 | 408 MiB | 302 MiB | 2614 MiB | 600 MiB | 3923 MiB | yes |
| classroom-8 | 32768 | 8 | 4096 | 544 MiB | 402 MiB | 2614 MiB | 600 MiB | 4160 MiB | yes |
| solo-demo | 8192 | 2 | 4096 | 136 MiB | 100 MiB | 2614 MiB | 600 MiB | 3450 MiB | yes |

> Compute-buffer column is PROVISIONAL (600 MiB at `-ub 512`, scaled linearly). [MEASURE: RSS delta across the `-ub` sweep on the target box, T13] — re-run with `--buffers-mib` once measured.

