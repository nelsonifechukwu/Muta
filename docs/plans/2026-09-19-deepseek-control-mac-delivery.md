# Untouched DeepSeek control GGUF delivery

The user asks for the DeepSeek model on their Mac, following their explicit GGUF question. Export the already acquired official checkpoint at revision `ad9f0ae0864d7fbcd1cd905e3c6c5b069cc8b562` to Q4_K_M with the existing pinned llama.cpp build, without applying any Muta adapter. Perform conversion/quantization on Oracle CPU with CUDA hidden, then transfer exactly one final GGUF to `models/science-tutor/controls/` on the Mac. Preserve official LICENSE and acquisition/export/download receipts. Do not copy the 3.55 GB training checkpoint or BF16 intermediate to the Mac.

Verify all seven official input file identities against the completed acquisition receipt before and after export. Record executed exporter/converter/quantizer hashes and exact commands, native GGUF metadata, intermediate and final checksums. Refuse an existing output directory; preserve failed logs. The local final checksum must equal the remote checksum before declaring delivery. This is a format conversion of control C4, not a new training experiment or a quality result. It may run during GPU training because both export commands are CPU-only and use a bounded eight-thread quantizer.
