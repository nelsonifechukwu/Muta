# Science tutor campaign — 19 September 2026

## Authority and scope

The user requests dataset construction first, then parallel fine-tuning of the main previous contenders, expansion/refinement of the strongest candidates, and matched holdout, 100 STEM and judges testing to select a practical GGUF winner. They explicitly authorize using both GPU systems with maximum useful parallelism. This replaces the advisory R1–R4 sequence, not the immutable evidence from earlier campaigns. No publishing private data, deleting old evidence, or swapping deployment defaults.

## Stages and decisions

User addition: the current roster is `provenance/science-tutor-20260919/EXPERIMENTS.md`. DeepSeek-R1-Distill-Qwen-1.5B adds a fourth fresh-LoRA pilot and unchanged control under the separate `2026-09-19-deepseek-science-contender.md` compatibility plan. The three original Qwen-lineage initializations below remain unchanged, not silently broadened to a different tokenizer/parent.

1. Build a pilot science/tutoring mixture targeting 20–40K genuinely distinct conversations/problems where admissible content permits. Pin exact public source revisions, hashes and licenses; preserve private restrictions. Cover conceptual, causal, quantitative, experimental, misconception, adaptive dialogue, scientific judgment and accessible explanation capabilities. Record gaps instead of fabricating coverage or padding counts.
2. Freeze source/group-disjoint development and final holdout sets before training. Keep source test partitions out of training. Exclude known judges, 100 STEM, existing 2,000 practical questions and their near duplicates from new training. Original source problem/conversation groups never cross splits, including derived turns. Report overlap limits; lexical screening alone is not proof of family independence.
3. Independently review pipeline and an explicitly reported stratified content audit. Numeric checks and source answer keys are different quality tiers; neither automatically validates all scientific explanations. Unresolved contradictions, missing visuals, missing answers or uncertain licensing are quarantined. No claim of universal row-by-row human approval.
4. Three matched pilot initializations: untouched Qwen2.5-1.5B-Instruct with fresh LoRA; original recovered Muta parent with fresh LoRA; exact best 20K warm-r16-lr5e6 adapter loaded once on its original parent with a fresh optimizer. Retain all three unchanged controls. Dataset, effective batch, seed, trainable-token budget and evaluation match as far as possible; record host/runtime differences.
5. Evaluate generated responses on development correctness, reasoning and tutoring, not only validation loss. Expand the audited mixture for at most two finalists and continue those selected checkpoints with a separately frozen stage config. Expansion target is quality/coverage-driven, not a mandatory 100K or 300K quota. Do not use final holdout or known-suite answers for adaptive training selection.
6. Freeze finalists and score held-out science/tutoring, the existing 2,000 practical battery, 100 STEM and judges prompts with identical GGUF runtime settings. Preserve original strict scores separately from semantic correctness, tutoring rubrics and format failures. Freeze exact rubrics and promotion rules before inference. Recommend retaining an unchanged control if no refinement improves the agreed quality/regression criteria. No universal winner or target-CPU score claim.

## Data and training interface

New code lives under `model-development/science_tutor/`; never modify frozen round-two sources. Private source/cache and generated datasets live under `data/muta-science-tutor-20260919/`, with one raw source copy and one canonical artifact per stage; manifests/provenance under `provenance/science-tutor-20260919/`. Rows use `id`, `group_id`, `source`, `source_revision`, `source_id`, `license`, `subject`, `capabilities`, `quality`, and full `messages` arrays. Real alternating user/assistant interactions are retained; only assistant output tokens receive loss. All user/system/history text is excluded from loss. Long dialogues must be split only at complete turn boundaries with preserved grouping, not silently truncated or counted as independent scenarios.

New multi-turn tokenizer must have explicit masking/role/boundary tests and independent review. Training manifests include source hashes, exact row/conversation/assistant-turn/token counts, optimizer settings, environment, parent and adapter identities, loss curves, checkpoint choices and exclusions. Export and evaluation have separate admission receipts. Failures are retained and diagnosed before an explicitly recorded retry.

## Compute and ownership

Oracle: `ubuntu@129.213.31.157`, existing campaign root `/lambda/nfs/awf-tmp/muta/campaign-20260918`. New run namespace `science-tutor-20260919`, existing shared GPU lock. Verified at 16:34 UTC: one idle A100 40GB. CSD3: `csd3`, root `/rds/user/ein21/hpc-work/muta-round2`; check authentication, credit balance, Slurm capacity before submission. Never resubmit old cancelled job 35801870 or disturb other work. Prefer one independent trial per available GPU; adapt microbatch/accumulation to preserve effective batch rather than oversubscribing a single device. Benchmark a bounded training calibration after dataset readiness, then revise ETA. Initial 5–10-hour estimate is planning only, not a promise of completion before midnight.

Root owns integration, source admission, benchmark freezing and final decision. Separate implementers own source adapters and training code; fresh-context reviewers must attempt to break the data/training/evaluation joins before launch. Independent source ingestion and code preparation can run in parallel; no training before dataset admission.
