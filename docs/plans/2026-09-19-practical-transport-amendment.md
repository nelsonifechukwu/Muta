# Pre-battery transport amendment

Written after the separate 16-response arithmetic smoke and before any of the
2,000 prospective questions were sent to a model. The original protocol,
executable and failed attempt remain unchanged. This supplements the transport
gate only; it does not change any question, answer, numeric grader, ranking,
promotion threshold, model roster, token budget or eight-slot setting.

The first smoke failed its strict byte-identity requirement: 15 responses were
identical (26 tokens), and one used different correct wording (41 tokens).
All 16 computed 43 and selected B, were nonempty and stopped normally. The
variant did not reproduce the requested ANSWER marker; this is recorded, not
relabelled as format compliance. Observed variation is consistent with backend
or dynamic-batching nondeterminism; its physical cause is not established.

An external reconciliation may accept this bounded transport outcome only
after independent review of all 16 request/raw/result joins, complete queue
disposition, pre/post artifact and code/runtime hashes, and actual logs showing
eight slots, 4,096 context each and 29/29 layers offloaded. It must retain the
original FAILED receipt and all raw bytes and label its status as accepted
non-bitwise-repeatable transport, never duplicate-integrity passed. No repeat
inference or selective output replacement is needed or allowed by this
amendment. No request corruption was found in inspected joins; that is not a
proof that corruption or all runtime variation is impossible.

The prospective five-model comparison remains one matched-setting realization.
Its cluster-bootstrap intervals measure item-cluster uncertainty only, not
inference-repeatability uncertainty. Disclose that qualification alongside
the reported ranking and bounds. The unchanged conservative recommendation rule
still applies, and all fresh format failures remain failures. This supports a
practical recommendation, not a bitwise-reproducibility or universal-winner claim.

Independent pre-inference source/method review: /root/five_manifest gave
conditional GO to this scope, requiring the actual reconciliation checks.
